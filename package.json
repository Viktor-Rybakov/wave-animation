{
  "private": "true",
  "scripts": {
    "stylesMinify": "rm -rf ./styles/index.min.css && npx postcss ./styles/index.css --use postcss-import --use autoprefixer --use postcss-csso --no-map --output ./styles/index.min.css"
  },
  "browserslist": [
    "last 2 version",
    "not dead",
    "not ie <= 11",
    "iOS >= 12"
  ],
  "devDependencies": {
    "autoprefixer": "^10.2.5",
    "postcss": "^8.2.4",
    "postcss-cli": "^8.3.1",
    "postcss-csso": "^5.0.0",
    "postcss-import": "^14.0.0"
  }
}
